# CodiON Frontend (UI-only scaffold)

## Run

```bash
npm i
npm run dev
```

## Notes
- Tailwind is configured in build mode (no CDN).
- Alias `@/*` points to `src/*`.
- This project is UI-only; add `react-router-dom` and your router later.
